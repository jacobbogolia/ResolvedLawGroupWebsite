---
export interface Props {
  content: string;
}

const { content } = Astro.props;
---

<p class="blog-paragraph">{content}</p>

<style>
  .blog-paragraph {
    font-family: var(--font-secondary);
    font-size: 16px;
    font-weight: 400;
    line-height: 1.8;
    color: var(--light-text);
    margin: 0 0 32px 0;
  }
</style>
